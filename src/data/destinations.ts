import type { Destination } from '@/types/travel';

// Les destinations sont désormais fournies par la base Neon.
// On expose ici un tableau vide pour conserver la compatibilité
// avec les anciens imports sans divulguer de données sensibles.
export const destinations: Destination[] = [];
